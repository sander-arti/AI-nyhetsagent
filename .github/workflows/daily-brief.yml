name: Daily AI Brief

on:
  schedule:
    # Run every day at 07:30 UTC (09:30 CEST / 08:30 CET)
    - cron: '30 7 * * *'
  workflow_dispatch: # Allow manual triggers for testing

jobs:
  run-ai-brief:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Start ChromaDB
      run: |
        docker run -d --name chromadb -p 8000:8000 chromadb/chroma:latest
        # Wait for ChromaDB to be ready
        sleep 10

    - name: Run AI Brief Pipeline
      env:
        YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
        LOOKBACK_HOURS: 24
        MAX_TRANSCRIPTION_MINUTES: 180
        SIMILARITY_THRESHOLD: 0.85
        MAX_ITEMS_PART1: 12
        MAX_ITEMS_PART2: 6
        MAX_ITEMS_PART3: 8
        TZ: Europe/Oslo
        NODE_ENV: github-actions
        SQLITE_DB_PATH: ./ai-agent.sqlite3
      run: |
        # Create data directory and initialize database
        mkdir -p data
        npx tsx scripts/migrate.ts
        npx tsx src/db/seed-sources.ts
        npx tsx src/index.ts

    - name: Upload logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: pipeline-logs
        path: |
          *.log
          logs/
        retention-days: 7